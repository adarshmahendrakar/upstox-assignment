/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import { HoldingScreens } from './src/Screens/HoldingsScreen';

AppRegistry.registerComponent(appName, () => HoldingScreens);
